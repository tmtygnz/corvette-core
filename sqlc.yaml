version: "2"

sql:
    - engine: "sqlite"
      schema: "./migrations"
      queries: "./queries"
      gen:
          go:
              package: "database"
              out: "./internal/database"
              emit_json_tags: true
              emit_interface: true
              emit_empty_slices: true
